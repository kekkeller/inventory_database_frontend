export default interface WizardStep {
    id: string;
    title: string;
    status?: 'complete' | 'current' | 'upcoming';
    buttonText?: string;
    buttonType?: 'button' | 'submit';
    buttonIcon?: string;
}
